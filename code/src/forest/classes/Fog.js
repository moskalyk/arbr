
// spectrum signature module
class Fog {

	constructor(fox, cptreeAddress){ 
		// compose the fox in the fog
		this.fox = fox
		this.cptree = cptreeAddress
	}

	compress () {

		// perform a read on the localized gather info
		const matrix = this.fox.hydrate()

		// load spectrum curation from the matrix into an fft bucket analyzer

		// take buckets and align mappings

		// extract visual signature
		const signature = {}

		return signature
	}
}

export default Fog